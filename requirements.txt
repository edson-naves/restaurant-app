# Pinned to versions verified against Python 3.14 (cp314 wheels available).
# Note: starlette >= 1.3 removed the legacy TemplateResponse(name, context)
# signature; app code uses the (request, name, context) form it requires.
fastapi==0.139.2
starlette==1.3.1
uvicorn[standard]==0.51.0
sqlalchemy==2.0.51
jinja2==3.1.6
python-multipart==0.0.32
python-dotenv==1.2.2

# tests
httpx==0.28.1
