# Menu item photos.
# Drop image files here (jpg/png/webp), then tell Claude which file maps to which
# menu item. They are served at /static/img/menu/<filename> and bind-mounted into
# the container, so new files appear live without a rebuild.
